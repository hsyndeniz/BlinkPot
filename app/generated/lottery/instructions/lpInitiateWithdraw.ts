/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  findConfigPda,
  findLpVaultPda,
  findPositionPda,
  findRoundCounterPda,
} from "../pdas";
import { LOTTERY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const LP_INITIATE_WITHDRAW_DISCRIMINATOR = new Uint8Array([
  245, 109, 245, 69, 132, 24, 172, 147,
]);

export function getLpInitiateWithdrawDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LP_INITIATE_WITHDRAW_DISCRIMINATOR,
  );
}

export type LpInitiateWithdrawInstruction<
  TProgram extends string = typeof LOTTERY_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoundCounter extends string | AccountMeta<string> = string,
  TAccountLpVault extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoundCounter extends string
        ? ReadonlyAccount<TAccountRoundCounter>
        : TAccountRoundCounter,
      TAccountLpVault extends string
        ? WritableAccount<TAccountLpVault>
        : TAccountLpVault,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      ...TRemainingAccounts,
    ]
  >;

export type LpInitiateWithdrawInstructionData = {
  discriminator: ReadonlyUint8Array;
  shares: bigint;
};

export type LpInitiateWithdrawInstructionDataArgs = { shares: number | bigint };

export function getLpInitiateWithdrawInstructionDataEncoder(): FixedSizeEncoder<LpInitiateWithdrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shares", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: LP_INITIATE_WITHDRAW_DISCRIMINATOR,
    }),
  );
}

export function getLpInitiateWithdrawInstructionDataDecoder(): FixedSizeDecoder<LpInitiateWithdrawInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shares", getU64Decoder()],
  ]);
}

export function getLpInitiateWithdrawInstructionDataCodec(): FixedSizeCodec<
  LpInitiateWithdrawInstructionDataArgs,
  LpInitiateWithdrawInstructionData
> {
  return combineCodec(
    getLpInitiateWithdrawInstructionDataEncoder(),
    getLpInitiateWithdrawInstructionDataDecoder(),
  );
}

export type LpInitiateWithdrawAsyncInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
  TAccountRoundCounter extends string = string,
  TAccountLpVault extends string = string,
  TAccountPosition extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config?: Address<TAccountConfig>;
  roundCounter?: Address<TAccountRoundCounter>;
  lpVault?: Address<TAccountLpVault>;
  position?: Address<TAccountPosition>;
  shares: LpInitiateWithdrawInstructionDataArgs["shares"];
};

export async function getLpInitiateWithdrawInstructionAsync<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TAccountRoundCounter extends string,
  TAccountLpVault extends string,
  TAccountPosition extends string,
  TProgramAddress extends Address = typeof LOTTERY_PROGRAM_ADDRESS,
>(
  input: LpInitiateWithdrawAsyncInput<
    TAccountOwner,
    TAccountConfig,
    TAccountRoundCounter,
    TAccountLpVault,
    TAccountPosition
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  LpInitiateWithdrawInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountRoundCounter,
    TAccountLpVault,
    TAccountPosition
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    roundCounter: { value: input.roundCounter ?? null, isWritable: false },
    lpVault: { value: input.lpVault ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.roundCounter.value) {
    accounts.roundCounter.value = await findRoundCounterPda();
  }
  if (!accounts.lpVault.value) {
    accounts.lpVault.value = await findLpVaultPda();
  }
  if (!accounts.position.value) {
    accounts.position.value = await findPositionPda({
      owner: expectAddress(accounts.owner.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roundCounter),
      getAccountMeta(accounts.lpVault),
      getAccountMeta(accounts.position),
    ],
    data: getLpInitiateWithdrawInstructionDataEncoder().encode(
      args as LpInitiateWithdrawInstructionDataArgs,
    ),
    programAddress,
  } as LpInitiateWithdrawInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountRoundCounter,
    TAccountLpVault,
    TAccountPosition
  >);
}

export type LpInitiateWithdrawInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
  TAccountRoundCounter extends string = string,
  TAccountLpVault extends string = string,
  TAccountPosition extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config: Address<TAccountConfig>;
  roundCounter: Address<TAccountRoundCounter>;
  lpVault: Address<TAccountLpVault>;
  position: Address<TAccountPosition>;
  shares: LpInitiateWithdrawInstructionDataArgs["shares"];
};

export function getLpInitiateWithdrawInstruction<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TAccountRoundCounter extends string,
  TAccountLpVault extends string,
  TAccountPosition extends string,
  TProgramAddress extends Address = typeof LOTTERY_PROGRAM_ADDRESS,
>(
  input: LpInitiateWithdrawInput<
    TAccountOwner,
    TAccountConfig,
    TAccountRoundCounter,
    TAccountLpVault,
    TAccountPosition
  >,
  config?: { programAddress?: TProgramAddress },
): LpInitiateWithdrawInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountConfig,
  TAccountRoundCounter,
  TAccountLpVault,
  TAccountPosition
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    roundCounter: { value: input.roundCounter ?? null, isWritable: false },
    lpVault: { value: input.lpVault ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roundCounter),
      getAccountMeta(accounts.lpVault),
      getAccountMeta(accounts.position),
    ],
    data: getLpInitiateWithdrawInstructionDataEncoder().encode(
      args as LpInitiateWithdrawInstructionDataArgs,
    ),
    programAddress,
  } as LpInitiateWithdrawInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountRoundCounter,
    TAccountLpVault,
    TAccountPosition
  >);
}

export type ParsedLpInitiateWithdrawInstruction<
  TProgram extends string = typeof LOTTERY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    config: TAccountMetas[1];
    roundCounter: TAccountMetas[2];
    lpVault: TAccountMetas[3];
    position: TAccountMetas[4];
  };
  data: LpInitiateWithdrawInstructionData;
};

export function parseLpInitiateWithdrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLpInitiateWithdrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      config: getNextAccount(),
      roundCounter: getNextAccount(),
      lpVault: getNextAccount(),
      position: getNextAccount(),
    },
    data: getLpInitiateWithdrawInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
