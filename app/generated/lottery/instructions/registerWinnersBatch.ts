/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { LOTTERY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REGISTER_WINNERS_BATCH_DISCRIMINATOR = new Uint8Array([
  131, 57, 183, 38, 220, 164, 182, 229,
]);

export function getRegisterWinnersBatchDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_WINNERS_BATCH_DISCRIMINATOR,
  );
}

export type RegisterWinnersBatchInstruction<
  TProgram extends string = typeof LOTTERY_PROGRAM_ADDRESS,
  TAccountTrigger extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTrigger extends string
        ? ReadonlySignerAccount<TAccountTrigger> &
            AccountSignerMeta<TAccountTrigger>
        : TAccountTrigger,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterWinnersBatchInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RegisterWinnersBatchInstructionDataArgs = {};

export function getRegisterWinnersBatchInstructionDataEncoder(): FixedSizeEncoder<RegisterWinnersBatchInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REGISTER_WINNERS_BATCH_DISCRIMINATOR,
    }),
  );
}

export function getRegisterWinnersBatchInstructionDataDecoder(): FixedSizeDecoder<RegisterWinnersBatchInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRegisterWinnersBatchInstructionDataCodec(): FixedSizeCodec<
  RegisterWinnersBatchInstructionDataArgs,
  RegisterWinnersBatchInstructionData
> {
  return combineCodec(
    getRegisterWinnersBatchInstructionDataEncoder(),
    getRegisterWinnersBatchInstructionDataDecoder(),
  );
}

export type RegisterWinnersBatchAsyncInput<
  TAccountTrigger extends string = string,
  TAccountConfig extends string = string,
  TAccountRound extends string = string,
> = {
  trigger: TransactionSigner<TAccountTrigger>;
  config?: Address<TAccountConfig>;
  round: Address<TAccountRound>;
};

export async function getRegisterWinnersBatchInstructionAsync<
  TAccountTrigger extends string,
  TAccountConfig extends string,
  TAccountRound extends string,
  TProgramAddress extends Address = typeof LOTTERY_PROGRAM_ADDRESS,
>(
  input: RegisterWinnersBatchAsyncInput<
    TAccountTrigger,
    TAccountConfig,
    TAccountRound
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterWinnersBatchInstruction<
    TProgramAddress,
    TAccountTrigger,
    TAccountConfig,
    TAccountRound
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trigger: { value: input.trigger ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.trigger),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.round),
    ],
    data: getRegisterWinnersBatchInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterWinnersBatchInstruction<
    TProgramAddress,
    TAccountTrigger,
    TAccountConfig,
    TAccountRound
  >);
}

export type RegisterWinnersBatchInput<
  TAccountTrigger extends string = string,
  TAccountConfig extends string = string,
  TAccountRound extends string = string,
> = {
  trigger: TransactionSigner<TAccountTrigger>;
  config: Address<TAccountConfig>;
  round: Address<TAccountRound>;
};

export function getRegisterWinnersBatchInstruction<
  TAccountTrigger extends string,
  TAccountConfig extends string,
  TAccountRound extends string,
  TProgramAddress extends Address = typeof LOTTERY_PROGRAM_ADDRESS,
>(
  input: RegisterWinnersBatchInput<
    TAccountTrigger,
    TAccountConfig,
    TAccountRound
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterWinnersBatchInstruction<
  TProgramAddress,
  TAccountTrigger,
  TAccountConfig,
  TAccountRound
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trigger: { value: input.trigger ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.trigger),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.round),
    ],
    data: getRegisterWinnersBatchInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterWinnersBatchInstruction<
    TProgramAddress,
    TAccountTrigger,
    TAccountConfig,
    TAccountRound
  >);
}

export type ParsedRegisterWinnersBatchInstruction<
  TProgram extends string = typeof LOTTERY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    trigger: TAccountMetas[0];
    config: TAccountMetas[1];
    round: TAccountMetas[2];
  };
  data: RegisterWinnersBatchInstructionData;
};

export function parseRegisterWinnersBatchInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterWinnersBatchInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      trigger: getNextAccount(),
      config: getNextAccount(),
      round: getNextAccount(),
    },
    data: getRegisterWinnersBatchInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
