/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { LOTTERY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REVEAL_DRAW_DISCRIMINATOR = new Uint8Array([
  13, 149, 75, 226, 184, 105, 114, 48,
]);

export function getRevealDrawDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REVEAL_DRAW_DISCRIMINATOR);
}

export type RevealDrawInstruction<
  TProgram extends string = typeof LOTTERY_PROGRAM_ADDRESS,
  TAccountTrigger extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountRandomnessAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTrigger extends string
        ? ReadonlySignerAccount<TAccountTrigger> &
            AccountSignerMeta<TAccountTrigger>
        : TAccountTrigger,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountRandomnessAccount extends string
        ? ReadonlyAccount<TAccountRandomnessAccount>
        : TAccountRandomnessAccount,
      ...TRemainingAccounts,
    ]
  >;

export type RevealDrawInstructionData = { discriminator: ReadonlyUint8Array };

export type RevealDrawInstructionDataArgs = {};

export function getRevealDrawInstructionDataEncoder(): FixedSizeEncoder<RevealDrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REVEAL_DRAW_DISCRIMINATOR }),
  );
}

export function getRevealDrawInstructionDataDecoder(): FixedSizeDecoder<RevealDrawInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevealDrawInstructionDataCodec(): FixedSizeCodec<
  RevealDrawInstructionDataArgs,
  RevealDrawInstructionData
> {
  return combineCodec(
    getRevealDrawInstructionDataEncoder(),
    getRevealDrawInstructionDataDecoder(),
  );
}

export type RevealDrawAsyncInput<
  TAccountTrigger extends string = string,
  TAccountConfig extends string = string,
  TAccountRound extends string = string,
  TAccountRandomnessAccount extends string = string,
> = {
  trigger: TransactionSigner<TAccountTrigger>;
  config?: Address<TAccountConfig>;
  round: Address<TAccountRound>;
  randomnessAccount: Address<TAccountRandomnessAccount>;
};

export async function getRevealDrawInstructionAsync<
  TAccountTrigger extends string,
  TAccountConfig extends string,
  TAccountRound extends string,
  TAccountRandomnessAccount extends string,
  TProgramAddress extends Address = typeof LOTTERY_PROGRAM_ADDRESS,
>(
  input: RevealDrawAsyncInput<
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountRandomnessAccount
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RevealDrawInstruction<
    TProgramAddress,
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountRandomnessAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trigger: { value: input.trigger ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    randomnessAccount: {
      value: input.randomnessAccount ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.trigger),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.randomnessAccount),
    ],
    data: getRevealDrawInstructionDataEncoder().encode({}),
    programAddress,
  } as RevealDrawInstruction<
    TProgramAddress,
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountRandomnessAccount
  >);
}

export type RevealDrawInput<
  TAccountTrigger extends string = string,
  TAccountConfig extends string = string,
  TAccountRound extends string = string,
  TAccountRandomnessAccount extends string = string,
> = {
  trigger: TransactionSigner<TAccountTrigger>;
  config: Address<TAccountConfig>;
  round: Address<TAccountRound>;
  randomnessAccount: Address<TAccountRandomnessAccount>;
};

export function getRevealDrawInstruction<
  TAccountTrigger extends string,
  TAccountConfig extends string,
  TAccountRound extends string,
  TAccountRandomnessAccount extends string,
  TProgramAddress extends Address = typeof LOTTERY_PROGRAM_ADDRESS,
>(
  input: RevealDrawInput<
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountRandomnessAccount
  >,
  config?: { programAddress?: TProgramAddress },
): RevealDrawInstruction<
  TProgramAddress,
  TAccountTrigger,
  TAccountConfig,
  TAccountRound,
  TAccountRandomnessAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trigger: { value: input.trigger ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    randomnessAccount: {
      value: input.randomnessAccount ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.trigger),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.randomnessAccount),
    ],
    data: getRevealDrawInstructionDataEncoder().encode({}),
    programAddress,
  } as RevealDrawInstruction<
    TProgramAddress,
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountRandomnessAccount
  >);
}

export type ParsedRevealDrawInstruction<
  TProgram extends string = typeof LOTTERY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    trigger: TAccountMetas[0];
    config: TAccountMetas[1];
    round: TAccountMetas[2];
    randomnessAccount: TAccountMetas[3];
  };
  data: RevealDrawInstructionData;
};

export function parseRevealDrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevealDrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      trigger: getNextAccount(),
      config: getNextAccount(),
      round: getNextAccount(),
      randomnessAccount: getNextAccount(),
    },
    data: getRevealDrawInstructionDataDecoder().decode(instruction.data),
  };
}
