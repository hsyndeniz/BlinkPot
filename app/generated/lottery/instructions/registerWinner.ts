/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { LOTTERY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REGISTER_WINNER_DISCRIMINATOR = new Uint8Array([
  136, 123, 88, 19, 249, 124, 0, 168,
]);

export function getRegisterWinnerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_WINNER_DISCRIMINATOR,
  );
}

export type RegisterWinnerInstruction<
  TProgram extends string = typeof LOTTERY_PROGRAM_ADDRESS,
  TAccountTrigger extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountTicket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTrigger extends string
        ? ReadonlySignerAccount<TAccountTrigger> &
            AccountSignerMeta<TAccountTrigger>
        : TAccountTrigger,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountTicket extends string
        ? WritableAccount<TAccountTicket>
        : TAccountTicket,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterWinnerInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RegisterWinnerInstructionDataArgs = {};

export function getRegisterWinnerInstructionDataEncoder(): FixedSizeEncoder<RegisterWinnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REGISTER_WINNER_DISCRIMINATOR }),
  );
}

export function getRegisterWinnerInstructionDataDecoder(): FixedSizeDecoder<RegisterWinnerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRegisterWinnerInstructionDataCodec(): FixedSizeCodec<
  RegisterWinnerInstructionDataArgs,
  RegisterWinnerInstructionData
> {
  return combineCodec(
    getRegisterWinnerInstructionDataEncoder(),
    getRegisterWinnerInstructionDataDecoder(),
  );
}

export type RegisterWinnerAsyncInput<
  TAccountTrigger extends string = string,
  TAccountConfig extends string = string,
  TAccountRound extends string = string,
  TAccountTicket extends string = string,
> = {
  trigger: TransactionSigner<TAccountTrigger>;
  config?: Address<TAccountConfig>;
  round: Address<TAccountRound>;
  ticket: Address<TAccountTicket>;
};

export async function getRegisterWinnerInstructionAsync<
  TAccountTrigger extends string,
  TAccountConfig extends string,
  TAccountRound extends string,
  TAccountTicket extends string,
  TProgramAddress extends Address = typeof LOTTERY_PROGRAM_ADDRESS,
>(
  input: RegisterWinnerAsyncInput<
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountTicket
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterWinnerInstruction<
    TProgramAddress,
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountTicket
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trigger: { value: input.trigger ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    ticket: { value: input.ticket ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.trigger),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.ticket),
    ],
    data: getRegisterWinnerInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterWinnerInstruction<
    TProgramAddress,
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountTicket
  >);
}

export type RegisterWinnerInput<
  TAccountTrigger extends string = string,
  TAccountConfig extends string = string,
  TAccountRound extends string = string,
  TAccountTicket extends string = string,
> = {
  trigger: TransactionSigner<TAccountTrigger>;
  config: Address<TAccountConfig>;
  round: Address<TAccountRound>;
  ticket: Address<TAccountTicket>;
};

export function getRegisterWinnerInstruction<
  TAccountTrigger extends string,
  TAccountConfig extends string,
  TAccountRound extends string,
  TAccountTicket extends string,
  TProgramAddress extends Address = typeof LOTTERY_PROGRAM_ADDRESS,
>(
  input: RegisterWinnerInput<
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountTicket
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterWinnerInstruction<
  TProgramAddress,
  TAccountTrigger,
  TAccountConfig,
  TAccountRound,
  TAccountTicket
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trigger: { value: input.trigger ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    ticket: { value: input.ticket ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.trigger),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.ticket),
    ],
    data: getRegisterWinnerInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterWinnerInstruction<
    TProgramAddress,
    TAccountTrigger,
    TAccountConfig,
    TAccountRound,
    TAccountTicket
  >);
}

export type ParsedRegisterWinnerInstruction<
  TProgram extends string = typeof LOTTERY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    trigger: TAccountMetas[0];
    config: TAccountMetas[1];
    round: TAccountMetas[2];
    ticket: TAccountMetas[3];
  };
  data: RegisterWinnerInstructionData;
};

export function parseRegisterWinnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterWinnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      trigger: getNextAccount(),
      config: getNextAccount(),
      round: getNextAccount(),
      ticket: getNextAccount(),
    },
    data: getRegisterWinnerInstructionDataDecoder().decode(instruction.data),
  };
}
